import { Typography } from "@mui/material";

export const Home = () => {
  return (
    <div>
      <Typography variant="h4">Welcome to the Dashboard!</Typography>
      <Typography>This is where the main content will be displayed.</Typography>
    </div>
  );
};